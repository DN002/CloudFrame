package dev.cloudframe.fabric.content;

import net.minecraft.block.Block;

/**
 * Placeholder power source block.
 *
 * For now this is just a normal block (no GUI) that Cloud Cables can connect to.
 * Future work can add animations (3-spoke propeller) and better visuals.
 */
public class CloudTurbineBlock extends Block {

    public CloudTurbineBlock(Settings settings) {
        super(settings);
    }
}
